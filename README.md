# blogpost

To run the frontend 
- npm install 
- npn start 



To run the backend 
- npm install -g nodemon 
- npm install
- nodemon app.js


backend will run on port 4000
forntend will run on port 3000 
